<!--dist/components/line.wxml-->
<view wx:if="{{opts.length>0}}" wx:for="{{opts}}" wx:key="key" wx:for-index='idx'>
  图表{{item.key}}
  <view style='width:100vw;height:300px;background-color:#eee'>
    <ff-canvas canvas-id="{{idx + 'p'}}" opts="{{item}}"></ff-canvas>
  </view>
</view>

